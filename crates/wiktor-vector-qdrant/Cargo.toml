[package]
name = "wiktor-vector-qdrant"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
description = "The Wiktor qdrant vector-backend plugin: the external vector service backing hybrid retrieval."
license.workspace = true
repository.workspace = true
documentation = "https://docs.rs/wiktor-vector-qdrant"
keywords = ["vector", "qdrant", "search", "embedding"]
categories = ["database"]
# 依赖规则（MASTER-PLAN §8）：插件只依赖 core，不依赖 server/feedback。
# Dependency rule (MASTER-PLAN §8): a plugin depends only on core, not on
# server/feedback.
[dependencies]
wiktor-core = { path = "../wiktor-core" }
qdrant-client = { workspace = true }
async-trait = "0.1"
blake3 = { workspace = true }
uuid = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["full"] }